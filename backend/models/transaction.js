/**
 * models/transaction.js
 * =======================
 * Defines the shape of a Transaction object.
 *
 * Schema:
 *   id          {number}  — auto-incremented unique identifier
 *   description {string}  — short text label for the transaction
 *   amount      {number}  — positive monetary value
 *   type        {string}  — 'income' | 'expense'
 *   date        {string}  — ISO date string (YYYY-MM-DD)
 *   createdAt   {string}  — ISO timestamp at insertion time
 */

// Example factory function (plain JS, no ORM):
// function createTransaction({ description, amount, type, date }) {
//   return {
//     id          : Date.now(),
//     description,
//     amount      : parseFloat(amount),
//     type,
//     date,
//     createdAt   : new Date().toISOString()
//   };
// }
// module.exports = { createTransaction };

// --- Placeholder ---
console.log('models/transaction.js — not yet implemented.');
